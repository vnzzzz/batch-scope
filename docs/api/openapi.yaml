components:
  schemas:
    AnalysisCycle:
      additionalProperties: false
      properties:
        containsImplicitRelation:
          type: boolean
        containsUncertainRelation:
          type: boolean
        cycleId:
          type: string
        nodes:
          items:
            $ref: "#/components/schemas/AnalysisNode"
          type: array
        route:
          items:
            $ref: "#/components/schemas/AnalysisCycleStep"
          type: array
      required:
        - cycleId
        - nodes
        - route
        - containsImplicitRelation
        - containsUncertainRelation
      type: object
    AnalysisCycleStep:
      additionalProperties: false
      properties:
        fromId:
          type: string
        toId:
          type: string
        viaRelations:
          items:
            $ref: "#/components/schemas/AnalysisRelation"
          type: array
        viaScope:
          type: boolean
      required:
        - fromId
        - toId
        - viaRelations
        - viaScope
      type: object
    AnalysisEvidence:
      additionalProperties: false
      properties:
        location:
          $ref: "#/components/schemas/AnalysisEvidenceLocation"
        note:
          type: string
        source:
          type: string
      required:
        - source
      type: object
    AnalysisEvidenceLocation:
      additionalProperties: false
      properties:
        endLine:
          format: int64
          type: integer
        jsonPointer:
          type: string
        startLine:
          format: int64
          type: integer
      type: object
    AnalysisFact:
      additionalProperties: false
      properties:
        businessDayOffset:
          format: int64
          type: integer
        certainty:
          enum:
            - declared
            - confirmed
            - inferred
            - candidate
          type: string
        duration:
          type: string
        id:
          type: string
        kind:
          enum:
            - finish_by
            - max_elapsed
            - raw
          type: string
        localTime:
          type: string
        origin:
          enum:
            - scheduler
            - deterministic_analysis
            - ai_analysis
            - manual
          type: string
        sourceText:
          type: string
        timeZone:
          type: string
      required:
        - id
        - kind
        - origin
        - certainty
      type: object
    AnalysisFinishByGroup:
      additionalProperties: false
      properties:
        items:
          items:
            $ref: "#/components/schemas/AnalysisLimitItem"
          type: array
        timeZone:
          type: string
        total:
          format: int64
          type: integer
      required:
        - timeZone
        - total
        - items
      type: object
    AnalysisHiddenConnection:
      additionalProperties: false
      properties:
        fromId:
          type: string
        toId:
          type: string
        viaRelations:
          items:
            $ref: "#/components/schemas/AnalysisRelation"
          type: array
        viaScope:
          type: boolean
      required:
        - fromId
        - toId
        - viaRelations
        - viaScope
      type: object
    AnalysisLimitGroup:
      additionalProperties: false
      properties:
        items:
          items:
            $ref: "#/components/schemas/AnalysisLimitItem"
          type: array
        total:
          format: int64
          type: integer
      required:
        - total
        - items
      type: object
    AnalysisLimitItem:
      additionalProperties: false
      properties:
        alternatePathCount:
          format: int64
          type: integer
        fact:
          $ref: "#/components/schemas/AnalysisFact"
        limitOwner:
          $ref: "#/components/schemas/AnalysisNode"
        scopeRoot:
          $ref: "#/components/schemas/AnalysisNode"
        treeNodeId:
          type: string
      required:
        - limitOwner
        - fact
        - treeNodeId
        - alternatePathCount
      type: object
    AnalysisLimitSections:
      additionalProperties: false
      properties:
        contained:
          $ref: "#/components/schemas/AnalysisLimits"
        downstream:
          $ref: "#/components/schemas/AnalysisLimits"
        target:
          $ref: "#/components/schemas/AnalysisLimits"
      required:
        - target
        - contained
        - downstream
      type: object
    AnalysisLimits:
      additionalProperties: false
      properties:
        finishByGroups:
          items:
            $ref: "#/components/schemas/AnalysisFinishByGroup"
          type: array
        maxElapsed:
          $ref: "#/components/schemas/AnalysisLimitGroup"
        raw:
          $ref: "#/components/schemas/AnalysisLimitGroup"
      required:
        - finishByGroups
        - maxElapsed
        - raw
      type: object
    AnalysisNode:
      additionalProperties: false
      properties:
        id:
          type: string
        name:
          type: string
        type:
          enum:
            - management_unit
            - job_network
            - job
            - file
            - file_pattern
            - job_status
            - external_event
          type: string
      required:
        - id
        - type
        - name
      type: object
    AnalysisRelation:
      additionalProperties: false
      properties:
        certainty:
          enum:
            - declared
            - confirmed
            - inferred
            - candidate
          type: string
        evidence:
          items:
            $ref: "#/components/schemas/AnalysisEvidence"
          type: array
        kind:
          enum:
            - precedes
            - produces
            - triggers
            - consumed_by
            - observed_by
          type: string
        origin:
          enum:
            - scheduler
            - deterministic_analysis
            - ai_analysis
            - manual
          type: string
      required:
        - kind
        - origin
        - certainty
      type: object
    AnalysisResponse:
      additionalProperties: false
      properties:
        bootId:
          type: string
        cycles:
          items:
            $ref: "#/components/schemas/AnalysisCycle"
          type: array
        limits:
          $ref: "#/components/schemas/AnalysisLimitSections"
        snapshotId:
          type: string
        target:
          $ref: "#/components/schemas/Details"
        tree:
          $ref: "#/components/schemas/AnalysisTreeNode"
        uncoveredRoutes:
          items:
            $ref: "#/components/schemas/AnalysisUncoveredRoute"
          type: array
      required:
        - bootId
        - snapshotId
        - target
        - limits
        - tree
        - uncoveredRoutes
        - cycles
      type: object
    AnalysisTreeNode:
      additionalProperties: false
      properties:
        children:
          items:
            $ref: "#/components/schemas/AnalysisTreeNode"
          type: array
        cycleId:
          type: string
        hiddenConnections:
          items:
            $ref: "#/components/schemas/AnalysisHiddenConnection"
          type: array
        hiddenJobCount:
          format: int64
          type: integer
        hiddenNodeIds:
          items:
            type: string
          type: array
        hiddenNodeIdsTruncated:
          type: boolean
        node:
          $ref: "#/components/schemas/AnalysisNode"
        referenceTo:
          type: string
        referenceType:
          enum:
            - shared
            - cycle
          type: string
        treeNodeId:
          type: string
        viaRelations:
          items:
            $ref: "#/components/schemas/AnalysisRelation"
          type: array
        viaScope:
          type: boolean
      required:
        - treeNodeId
        - node
        - children
      type: object
    AnalysisUncoveredRoute:
      additionalProperties: false
      properties:
        boundary:
          $ref: "#/components/schemas/AnalysisNode"
        cycleId:
          type: string
        reason:
          enum:
            - terminal_without_limit
            - cycle_without_limit
            - non_traversable_node_type
          type: string
        treeNodeId:
          type: string
      required:
        - boundary
        - reason
        - treeNodeId
      type: object
    Ancestor:
      additionalProperties: false
      properties:
        id:
          type: string
        name:
          type: string
        type:
          enum:
            - management_unit
            - job_network
          type: string
      required:
        - id
        - type
        - name
      type: object
    BuildInfo:
      additionalProperties: false
      properties:
        commit:
          type: string
        version:
          type: string
      required:
        - version
        - commit
      type: object
    Details:
      additionalProperties: false
      properties:
        ancestorPath:
          items:
            $ref: "#/components/schemas/Ancestor"
          type: array
        id:
          type: string
        name:
          type: string
        path:
          type: string
        type:
          enum:
            - job
            - job_network
          type: string
      required:
        - ancestorPath
        - id
        - type
        - name
      type: object
    ErrorDetail:
      additionalProperties: false
      properties:
        location:
          description: Where the error occurred, e.g. 'body.items[3].tags' or 'path.thing-id'
          type: string
        message:
          description: Error message text
          type: string
        value:
          description: The value at the given location
      type: object
    ErrorModel:
      additionalProperties: false
      properties:
        detail:
          description: A human-readable explanation specific to this occurrence of the problem.
          examples:
            - Property foo is required but is missing.
          type: string
        errors:
          description: Optional list of individual error details
          items:
            $ref: "#/components/schemas/ErrorDetail"
          type:
            - array
            - "null"
        instance:
          description: A URI reference that identifies the specific occurrence of the problem.
          examples:
            - https://example.com/error-log/abc123
          format: uri
          type: string
        status:
          description: HTTP status code
          examples:
            - 400
          format: int64
          type: integer
        title:
          description: A short, human-readable summary of the problem type. This value should not change between occurrences of the error.
          examples:
            - Bad Request
          type: string
        type:
          default: about:blank
          description: A URI reference to human-readable documentation for the error.
          examples:
            - https://example.com/errors/example
          format: uri
          type: string
      type: object
    HealthResponse:
      additionalProperties: false
      properties:
        status:
          type: string
      required:
        - status
      type: object
    ReadyResponse:
      additionalProperties: false
      properties:
        reason:
          type: string
        status:
          type: string
      required:
        - status
        - reason
      type: object
    SearchItem:
      additionalProperties: false
      properties:
        ancestorPath:
          items:
            $ref: "#/components/schemas/Ancestor"
          type: array
        id:
          type: string
        matchedBy:
          items:
            enum:
              - id
              - name
              - path
            type: string
          type: array
        name:
          type: string
        path:
          type: string
        type:
          enum:
            - job
            - job_network
          type: string
      required:
        - matchedBy
        - ancestorPath
        - id
        - type
        - name
      type: object
    StatusResponse:
      additionalProperties: false
      properties:
        bootId:
          type: string
        build:
          $ref: "#/components/schemas/BuildInfo"
        snapshot: {}
        startedAt:
          format: date-time
          type: string
        state:
          type: string
      required:
        - state
        - bootId
        - startedAt
        - snapshot
        - build
      type: object
    TargetsResponse:
      additionalProperties: false
      properties:
        items:
          items:
            $ref: "#/components/schemas/SearchItem"
          type: array
        snapshotId:
          type: string
        truncated:
          type: boolean
      required:
        - snapshotId
        - items
        - truncated
      type: object
info:
  title: BatchScope API
  version: v1
openapi: 3.1.0
paths:
  /healthz:
    get:
      operationId: health
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthResponse"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Check process health
  /readyz:
    get:
      operationId: readiness
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReadyResponse"
          description: OK
        "503":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReadyResponse"
          description: Service Unavailable
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Check snapshot readiness
  /v1/downstream-limit-analysis:
    get:
      operationId: analyze-downstream-limits
      parameters:
        - description: Exact job or job network ID to analyze
          explode: false
          in: query
          name: targetId
          required: true
          schema:
            description: Exact job or job network ID to analyze
            type: string
        - description: Include relation evidence in the response
          explode: false
          in: query
          name: includeEvidence
          schema:
            default: false
            description: Include relation evidence in the response
            type: boolean
        - description: Request identifier used in structured logs
          in: header
          name: X-Request-Id
          schema:
            description: Request identifier used in structured logs
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AnalysisResponse"
          description: OK
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Bad Request
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Not Found
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Internal Server Error
        "503":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Service Unavailable
      summary: Analyze downstream configured limits and dependency routes
  /v1/status:
    get:
      operationId: status
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StatusResponse"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Get service status
  /v1/targets:
    get:
      operationId: search-targets
      parameters:
        - description: Exact ID, name, or full path to search for
          explode: false
          in: query
          name: query
          required: true
          schema:
            description: Exact ID, name, or full path to search for
            type: string
        - description: Target type filter; may be repeated
          explode: true
          in: query
          name: type
          schema:
            description: Target type filter; may be repeated
            items:
              enum:
                - job
                - job_network
              type: string
            type:
              - array
              - "null"
        - description: Request identifier used in structured logs
          in: header
          name: X-Request-Id
          schema:
            description: Request identifier used in structured logs
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TargetsResponse"
          description: OK
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Bad Request
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Internal Server Error
        "503":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Service Unavailable
      summary: Search jobs and job networks by exact match
